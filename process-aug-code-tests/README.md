## Build Notes

* Snapshots of code-augmentor-core artifact must be published to Maven local cache for Groovy Grape mechanism to pick it up.
* change directory into tests.groovy directory before running tests with `groovy tests.groovy`
